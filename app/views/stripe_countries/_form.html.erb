<%= form_for(@stripe_country) do |f| %>
  <% if @stripe_country.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stripe_country.errors.count, "error") %> prohibited this stripe_country from being saved:</h2>

      <ul>
      <% @stripe_country.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :country_name %><br>
    <%= f.text_field :country_name %>
  </div>
  <div class="field">
    <%= f.label :country_code %><br>
    <%= f.text_field :country_code %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
